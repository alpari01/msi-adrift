version: "3"
services:
  msi-adrift-project:
    image: msi-adrift:test
    build: ../../.
    volumes:
      - output_volume:/output
    ports:
      - "8080:8080"
    command: ["pytest", "--cov=.", "--cov-report=term-missing", "test_app_unittest.py"]

volumes:
  output_volume:
